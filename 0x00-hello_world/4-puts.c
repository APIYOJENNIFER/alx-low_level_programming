#include <stdio.h>

/**
 * main - Use puts() to print text
 *
 * Return: 0 if success other non-positive integer
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

