#include <stdio.h>

/**
 * main - use printf() to print out text
 *
 * Return: 0 if success, otherwise non-positive integer
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

